# Language Server Protocol

The `lsp` package contains a small stdio JSON-RPC transport and lifecycle integration. Language servers are optional: ForgeIDE starts one only when a supported command is available on the machine.
